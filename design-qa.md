# 自动化任务与记忆提取设计验收

## 参考

- 视觉参考：`/Users/revan/.codex/attachments/c8917701-0844-4daa-bff3-91ee306a7d71/image-1.png`
- 实现页面：`http://127.0.0.1:5173/agent/tasks`
- 对照状态：新建自动化任务 -> 自动化类型 -> 记忆提取任务
- 目标视口：1440 x 960

## 功能与结构验收

- [x] 页面只保留自动化任务，不呈现任务触发洞察。
- [x] 弹窗首行是任务名称、任务状态；状态为运行中、暂停、终止。
- [x] 自动化类型仅有数据获取任务、记忆提取任务。
- [x] 数据获取任务只显示已测试成功的数据获取脚本，不显示主题表和已验证数据连接。
- [x] 数据获取任务展示近三次运行情况。
- [x] 记忆提取任务只展示知识文件，并提供 Prompt。
- [x] 输出区域包含分析经验、意图管理、分析习惯、运营习惯、汇报习惯。
- [x] 选中文件与输出项使用参考图的浅绿色背景、绿色边框和两列卡片结构。
- [x] 前端交互烟测覆盖类型切换、字段显隐、五类输出和旧入口移除。
- [x] 1440 x 960 DOM 几何验收通过：弹窗居中且不溢出，首行控件对齐，输入区两列、输出区三列、Prompt 满宽。
- [x] 选中态计算样式验收通过：背景 `rgb(238, 248, 241)`，边框 `rgb(205, 235, 213)`。

## 截图对照

- [x] 状态：通过。
- 浏览器：本机 Google Chrome 无头模式。
- 实现截图：`output/playwright/memory-task-modal-1440x960.png`；弹窗裁切：`output/playwright/memory-task-modal-dialog.png`。
- 同图对照：`output/playwright/memory-task-reference-vs-implementation.png`。
- 对照状态：1440 x 960 页面中打开“新建自动化任务”，切换为“记忆提取任务”，勾选两个知识文件并保留五类输出全选。
- 可见检查：弹窗完整居中，底部操作区可见；卡片两列、输出三列和全宽 Prompt 的间距、圆角、边框与现有设计系统一致，无裁切或横向溢出。
- 与参考图的有意差异：输入范围按最新要求收敛为知识文件，不再混入原始表和主题表；输出按最新要求由三类扩展为分析经验、意图管理、分析习惯、运营习惯、汇报习惯五类。

## 自动化证据

- `npm run typecheck`：通过。
- `npm run build`：通过。
- `npm run test:frontend-permissions`：通过。
- `npm test`：通过；291 个后端测试通过，2 个环境性跳过。

---

# 经营周报与多机构分析共享右栏设计验收

## 参考

- 去嵌套参考：`/var/folders/5j/1tdj8bb17jbc5z_08kq65_bc0000gn/T/codex-clipboard-68f8c3a4-4696-4711-b8b9-3468124fb7ff.png`
- 交互参考：`/var/folders/5j/1tdj8bb17jbc5z_08kq65_bc0000gn/T/codex-clipboard-9f541c5a-a6a6-4ea2-97f5-f215695f88a5.png`
- 实现页面：`http://127.0.0.1:5173/weekly-report`、`http://127.0.0.1:5173/`

## 视觉与交互验收

- [x] 共享右栏使用系统统一的白色容器、浅灰内容底色、`#e5e5ea` 分隔线和蓝色浅底选中态。
- [x] AI 分析卡初始不高亮；点击输入区后只高亮当前卡；点击卡片外部后高亮消失。
- [x] 点击另一张卡时切换为单卡高亮，未执行的临时选区卡同时清理。
- [x] 未执行且未自动触发的选区分析卡，点击页面其他位置后自动删除；总体分析卡不删除。
- [x] 完成按钮固定显示“已完成”，点击后当前 AI 分析卡直接从右栏移除，不再切换为“恢复”。
- [x] 经营周报选中“3.48亿”、多机构分析选中“1.31”后点击 AI 分析，浏览器原生选区消失后仍保留红色持久下划线；评论继续使用蓝色下划线，二者视觉语义不混用。
- [x] 点击红色下划线会反向激活且只高亮对应 AI 分析卡；右栏独立滚动并将对应卡片移动到选中文字的同一纵向高度。两个页面实测顶部误差均为 `0px`。
- [x] 同一内容模块的不同文字范围使用独立分析目标，避免后一次选区复用前一次分析卡。
- [x] 右上角折叠箭头会同时收起输入框和分析结果主体，再次点击可恢复。
- [x] 思考链移除运行时强制滚到底部，只响应用户在思考链区域内的滚轮滚动。
- [x] 思考链、参考数据、可视化分析、分析结论仅保留外层四页签边框；内部标题栏、卡片边框和冗余底色已移除。
- [x] 经营周报与多机构分析均使用同一共享组件并完成浏览器交互验收。
- [x] 评论数据按“机构（租户）+ 页面报告”共享：同机构有权限用户可查看全部评论、发表评论和回复；切换机构后不会串读评论。
- [x] AI 分析卡及结果按“机构（租户）+ 当前用户 + 页面报告”隔离；同机构其他用户与同用户其他机构均不能读取，切换用户或机构的首帧即清空旧卡片，不等待副作用回收。
- [x] 多机构分析已接入与经营周报一致的后端评论线程；评论在页面刷新后仍保留，后续页面可复用同一机构评论 Hook 与共享右栏权限边界。
- [x] 前端开发服务更新或短暂中断造成动态模块失效时，只在服务可达时自动重载一次；持续失败时显示系统风格中文恢复页，不再暴露 React Router 英文错误白屏。
- [x] 实时语音启动失败会区分 API 不可达、登录失效、权限不足和 Fun-ASR 配置/网络异常；文字分析不受影响，并提供“重新连接”入口。

## 自动化证据

- `npm run test:context-rail`：通过，47 项共享右栏契约全部通过；覆盖持久 AI 下划线、AI/评论颜色隔离、禁止旧紫色样式回归、双页面反向激活、机构评论复用、用户/机构 AI 首帧隔离、评论引用可读化和顶部顺序排列。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- `python3 -m unittest backend.platform.tests.test_context_rail_visibility`：通过；验证同机构跨用户评论可见与回复、跨机构评论不可见，以及 AI 任务跨用户/跨机构均返回 `403`，并覆盖评论 DELETE HTTP 路由。
- `npm test`：通过；292 个后端测试通过，2 个环境性跳过，前端权限冒烟通过。
- `python3 -m unittest backend.platform.tests.test_fun_asr_proxy`：通过，13 项 Fun-ASR 代理测试全部通过。
- `npm run test:frontend-permissions`：通过；隔离模拟音频、WebSocket、自动重连和转写分析链路均通过，不采集真实麦克风音频。
- 真实页面：`/weekly-report` 与 `/` 动态模块均加载完成，`/api/health` 前端代理和 API 直连均返回 `200`；最终重载无异常页，控制台仅保留修复过程发生于 `10:23:59Z` 的两条历史 Vite HMR 记录，当前生产构建与页面加载均已通过。

## 评论乱码与顶部排列复验（2026-07-18）

- source visual truth：`/var/folders/5j/1tdj8bb17jbc5z_08kq65_bc0000gn/T/codex-clipboard-6bde5b17-f3de-4b36-b8ea-114b23709cc3.png`
- implementation screenshot：`/Users/revan/Downloads/Data Agent/Smart_Data_Agent/artifacts/comment-rail-readable-top-aligned.png`
- combined comparison：`/Users/revan/Downloads/Data Agent/Smart_Data_Agent/artifacts/comment-rail-before-after.png`
- viewport：`1252 × 720`；对比区域为右侧评论栏 `320 × 688`。
- state：华兴银行、多机构分析、评论页签，包含一个新评论输入框和一个历史 JSON 评论；经营周报另以已有“+0.38亿 / 你好”评论复验。
- full-view comparison evidence：修复前引用区直接显示 JSON 且原生 title 弹层遮挡正文；修复后输入框和历史评论均显示“指标：值（变化）”中文摘要，页面无 JSON 字符串。
- focused region evidence：评论滚动区首卡顶部间距实测 `12px`，第二卡紧随第一卡向下排列，卡间距实测 `18px`；经营周报首卡顶部间距同为 `12px`。
- typography：沿用系统字体、字号和行高；摘要可换行并限制高度，没有长 JSON 破坏字距和换行。
- spacing/layout：评论不再按正文锚点制造空白，默认从顶部连续排列；卡片圆角、内边距和右栏宽度保持原系统规范。
- colors/tokens：继续使用系统白底、浅灰引用文字、黄色评论强调边和蓝色输入框，不引入新色值。
- image/assets：本次没有新增或替换图片资产；Lucide 现有评论、图片和操作图标保持不变。
- copy/content：指标、趋势、雷达、分行表格和洞察目标均由结构化数据转换为可读摘要；历史 JSON 评论在渲染时兼容转换，无需迁移数据库。
- comparison history：首轮发现 `[P1] JSON 序列化内容直接暴露并触发巨大原生提示层` 与 `[P1] 评论按正文锚点定位造成顶部空白`；已分别通过统一可读摘要函数和顶部顺序定位修复。复验截图、DOM 文本与位置测量均无残留 P0/P1/P2。
- primary interactions：多机构分析选择“消费贷核心指标”并打开评论输入框；切换至经营周报评论页签；历史评论与新评论引用均已检查。
- console errors：最终检查无当前页面错误。

final result: passed

---

# 评论时间格式复验（2026-07-18）

- [x] 经营周报与多机构分析共享评论组件统一把评论、追评时间格式化为 `YYYY-MM-DD HH:mm:ss`。
- [x] ISO 时间中的毫秒、`Z`、时区偏移和其他尾部信息不再显示。
- [x] 无法识别为日期时间的附加文字不再占用评论标题行。
- [x] `npm run test:context-rail` 新增评论时间格式约束并通过。

final result: passed

---

# 左侧栏与上下文标注联动复验（2026-07-18）

## 本轮需求验收

- [x] 左侧栏收起后，页面左侧全高 `40px` 区域均可唤出展开按钮；按钮中心跟随鼠标纵坐标并限制在视口内。浏览器实测鼠标从 `y=180` 移到 `y=520` 时，按钮顶部由 `162px` 移到 `502px`，位移同为 `340px`，点击后侧栏恢复展开。
- [x] 评论与 AI 分析使用两套独立标注语义：评论为蓝色下划线与蓝色浅底，AI 分析为红色下划线与红色浅底；经营周报与多机构分析共用一致规则。
- [x] 右栏收起时点击页面已有下划线会主动展开右栏、切换到“评论”或“AI 分析”页签，并将对应卡片滚动到可见位置；浏览器实测评论标注与评论卡片同时高亮，评论卡片顶部定位至 `156px`。
- [x] 评论作者由用户资料表中的显示名返回；历史 `u_super_admin` 评论在浏览器中显示为当前登录姓名“胥京波”，新评论和回复的乐观渲染也使用登录姓名，不再暴露用户 ID。
- [x] AI 分析输入框和结果区域统一收纳于最外层分析卡；浏览器计算样式实测外层卡边框为 `1px`，内部输入区边框为 `0px`，结果区只保留与外层卡衔接的顶部内容分隔线。

## 回归证据

- `npm run test:context-rail`：47 项全部通过，覆盖全高左侧热区、鼠标跟随、双页面两色标注、禁止旧紫色样式回归、下划线反向展开与定位、活动卡滚动、单层 AI 卡结构。
- `python3 -m unittest backend.platform.tests.test_report_integrity backend.platform.tests.test_context_rail_visibility`：6 项全部通过，覆盖评论显示名映射、同机构评论可见性和跨机构隔离。
- `npm test`：通过；292 个后端测试通过，2 个环境性跳过，前端构建、类型检查和权限冒烟测试均通过。
- 真实浏览器：经营周报评论下划线激活色为背景 `rgb(220, 233, 255)`、边框 `rgb(11, 87, 208)`；右栏从收起状态自动展开并显示对应高亮卡。AI 分析从收起状态打开后切换到正确页签，目标分析卡高亮且内部输入区无独立边框。

final result: passed

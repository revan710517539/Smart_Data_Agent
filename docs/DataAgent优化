如下是双周报数据分析过程中需要优化的内容，你需要结合项目的代码逻辑和架构逻辑，充分理解下面的各个优化项，从完整系统优化的角度完整的优化掉下面的几个问题，不要只改页面，也不要只改服务端，要从完整的架构角度出发，思考这些优化项如何体系化完成，完成的过程中要充分遵照项目架构，优化完成后要充分测测，从完整项目、完整架构、高稳定性、高完整性、高可用的角度测试并推动优化，直到优化完成为止。
1）经营分析一级菜单下添加邮件日报，放在机构督导菜单后面；经营沙盘和业务漏斗两个菜单调整顺序；
2）你整体改了一版之后把我的指标字典中的内容给我改没了，现在一个指标都没有，你需要给我复原回来，指标见附件；
3）数据管理菜单下原始表tab下没有SQL的逻辑，可以去掉，同时在每个表对应的子模块右上方添加编辑、保存、取消按钮，支持对对应子模块表中字段的修改，表中的字段英文、字段中文、类型三个字段不能修改，只能修改语义解释、示例用法两个字段。
4）主题表中的元数据字段都应的表格描述不完整，应该形成一个类似于原始表的表格及结构，同时在每个子模块的右上方添加编辑、保存、取消按钮，支持对对应子模块表中字段的修改，表中的字段英文、字段中文、类型三个字段不能修改，只能修改语义解释、示例用法两个字段。这个主题表对应的SQL不展示在每个子模块的下方，而是在编辑、保存、取消三个按钮的前面添加一个脚本按钮，点击按钮则展示出这个主题表的完整生成SQL，注意，主题表中引用的表是来自于原始表的，主题表的逻辑是用户在这个系统中通过AI生成的SQL，被证实有用后，记录在这里；原始表则是记录从其他平台引用过来的数据，主要针对QBI，可以与QBI做接口连通，获取QBI数据集中的元数据。
5）任务工作台菜单下二级菜单的顺序调整为：能力总览、实时洞察、自动化任务。
6）在智能分析页面的输入框语音录入的左边添加一个加号符号，点击加号符号则下拉框中显示文件上传、描述性分析、归因分析、预测分析、探索性分析、财务预算分析，选择文件上传时，则调起电脑文件夹找到要上传的文件，不支持直接上传文件夹，选择的文件以附件的形式呈现在输入框的上方紧贴输入框，文件名称不要太大，支持一次输入多个文件，作为数据分析的知识文件，这些知识文件传递与元数据信息同步传递给大模型支持数据分析；选择“周报分析、描述性分析、归因分析、预测分析、探索性分析、财务预算分析”时，这些文字直接显示在输入框内的左侧，作为特定主题下数据分析的skill，skill中封装了对应主题的分析思路和展示样式，开始进行数据分析时，这个skill随问题同步传递给大模型，一起进行分析，下拉框中上面内容主要分三类：添加、场景、主题，添加类别下是文件上传；场景类别下是周报分析、运营日常分析、风险策略分析；主题类别下是描述性分析、归因分析、预测分析、探索性分析、财务预算分析，不同类别下的各个内容都有一套特定逻辑的skill，帮助用户更深入分析；
7）在自动化任务二级菜单下方添加“Skill插件”，这个页面用来管理加载在这个系统中的skills，这些skills可以应用在智能分析、市场与客户洞察、经营分析、管理驾驶舱四个页面中。任务工作台页面标题：Data Agent工作台名称调整为任务工作台。
8）经营周报二级菜单中去掉页面上的保存时间所在子模块，去掉右上方的复制到双周报按钮；去掉“重点项目一”字样，会议时间、汇报人、报告周期三个子子模块调整为可以编辑，分析数据下拉框位置调整到“一、业绩与业务波动”水平位置的最右侧，下拉框的高度不变，但是宽度适当变小，跟下面重新生成的按钮宽度保持一致；双周报中点击已经做过评论的内容，则评论框移动到同一水平位置，评论框跟选中的文字位置上对齐；如果评论框内容过多，则评论框可适当折叠，将追评的内容全部折叠起来，折叠后右下方增加一个向下的双层箭头，点击打开后右下方的双层箭头变为向上的双层箭头，位置不动，点击向下的双层箭头则评论展开，点击向上的双层箭头则评论折叠。
余额达成情况与业务波动分析两个数据子模块的表格样式不完全一样，两个的样式要保持跟第一个一样。
9）自主分析中智能分析的菜单下，输入框背后的分析逻辑是一个完整的智能分析引擎，它需要结合你输入的内容、文件、skill、插件、元数据进行深入、完整、定主题的分析，最终输出SQL、Python、分析思路、可能的结论四块内容，并结合这四个内容，自动获取数据、用Python脚本渲染成可视化报表、结合分析思路给出分析结论，并呈现在页面上，这一逻辑不体现在系统页面上，但要在服务端独立一个模块来维护这个智能分析引擎。

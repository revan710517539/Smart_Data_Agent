【角色设定】
你是一位拥有十年以上经验的资深后端架构师，精通企业级系统的权限体系设计与Python后端开发。
【业务背景与需求】
我正在开发一个新系统，需要将一个独立的权限管理模块整合到项目中。系统承载多个独立的机构（多租户），每个机构内部拥有一套完全隔离的权限体系。具体业务规则如下：
1.多租户隔离：每个机构（Tenant）的数据和权限完全独立。
2.角色体系：每个机构下默认有“管理员”和“操作员”，机构管理员可自主创建新角色并分配权限，新角色的权限小于等于机构管理员的权限，即：机构管理员最大可以创建一个跟他同等权限的角色。
3.超级管理员特权：系统存在超级管理员，可以给同一个用户授予跨多个机构的权限（即同一用户在不同机构可拥有不同角色），且可以编辑每个用户、角色下的权限。
4.细粒度权限控制：权限范围涵盖五个维度：菜单访问、功能按钮、机构管理、数据指标（需实现行级/字段级数据隔离）、可管理角色（角色管理按照超级管理员、机构管理员、机构操作员三个层级，可管理的角色只能是权限小于或等于当前角色，不能大于）。
【技术实现要求】
请参照 Casbin 的 PERM 元模型设计思想，在我的 Python 项目中实现一套原生的权限管理模块。注意：不需要直接引入 Casbin 第三方库，而是要复刻其“模型与策略分离”的核心逻辑。
我们这次引用的参考项目有：https://github.com/casbin/pycasbin
【具体任务清单】
请分步骤为我提供以下设计方案与核心代码实现：
1.数据库表结构设计：
○ 设计符合多租户隔离的表结构，核心实体包括：机构表（Tenants）、用户表（Users）、角色表（Roles）、权限资源表（Resources/Permissions）、以及用户-角色关联表、角色-权限关联表。
○ 确保表结构能够支持“同一用户跨机构拥有不同角色”以及“数据指标”的属性存储。
2.权限模型定义（Model 层）：
○ 设计一个类 Casbin 的权限匹配模型。请定义请求对象（Request）的结构，必须包含：主体（用户ID）、域（机构ID）、资源（资源标识）、动作（如 read/write）、以及资源属性（用于数据指标过滤）。
○ 编写核心的权限匹配器（Matcher）逻辑伪代码，说明如何判断“用户在特定机构下的角色是否拥有对某资源的特定操作权限”。
3.策略引擎核心实现（Service 层）：
○ 用 Python 编写一个轻量级的权限决策引擎类（例如命名为 AuthEnforcer）。
○ 该类需包含加载策略（从数据库读取当前用户的角色与权限）、执行鉴权（enforce(user_id, tenant_id, resource, action) 方法返回布尔值）的核心逻辑。
○ 针对“数据指标”的权限，演示如何在鉴权时结合资源属性（如数据所属机构ID）进行动态过滤（体现 ABAC 思想）。
【要实现的逻辑】
角色权限菜单下的角色管理是为了限定有多少角色，每个角色有不同的权限，除管理员默认全权限且可以给其他角色授权外，其他角色都需要配置，配置的内容主要是：机构、菜单、数据、可管理角色四个：
2.1）机构下可以选择附件文件中的所有银行，菜单可以选择系统中的所有一级和二级菜单，
2.2）菜单下如果选择一级菜单时可以关联选中他项下的所有二级菜单，如果没有选择一级菜单，直接选择二级菜单，则显示出所选二级菜单，以及二级菜单对应的一级菜单，其他不显示；
2.3）系统管理菜单下的审计日志、系统配置两个菜单默认显示，所有角色都默认有；
2.4）数据是在每个角色下加挂对应指标，默认某机构的角色能查看这个机构下全量的指标，如果不允许角色看指标则去掉选择，本机构下管理员/操作员有查看、新增指标的权限，超级管理员有查看、新增、修改、删除指标的权限，各个机构新增的指标要添加新增人员的字段；
2.5）可管理角色下可以选择本机构操作员的角色，以及自己创建的新角色；
2.6）每个机构下角色默认角色有两个：管理员默认有管理员给这个机构下的全权限，操作员默认继承管理员除角色管理外的所有权限，管理员多个角色管理；
2.7）用户管理界面下保存了所有注册这个系统的客户，每个机构下都默认有个管理员账号，新增的账号有编辑和删除按钮，点击编辑可以修改他有的角色，点击添加用户时，挑起弹窗，要求录入相应的用户信息，同时添加需要的角色，角色只有两个：管理员、操作员以及管理员再新增的角色。
2.8）超级管理员进入系统后，能够添加数据源、添加机构，能够给他添加的用户授予看什么机构的权限，以及这个机构下授予管理员还是操作员，同时在用户管理、角色权限两个菜单下能看到当下机构下的所有用户和角色，页面要保持跟我们系统的风格一致。
